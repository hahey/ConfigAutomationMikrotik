/system script add name=ffconfig source={
/user set admin password=XXXXXXXXXXXXXXXXXXXx
:local strName "$street";
:local id "$floor0$idnum";
#:local strName "seehausener";
#:local id "123";
/system identity set name="$strName-$id";

/interface bridge add disabled=no name=bridge1
/interface bridge port add interface=ether1 bridge=bridge1
/interface bridge port add interface=wlan1 bridge=bridge1

/interface wireless set wlan1 disabled=no ssid="$strName.freifunk.net" frequency=2437 band=2ghz-b/g/n mode=ap-bridge
#/interface wireless set wlan1 disabled=no ssid="berlin.freifunk.net" frequency=2437 band=2ghz-b/g/n mode=ap-bridge

/ip dhcp-client remove numbers=0
#:if ([/interface dhcp-client find interface=ether1] != "") do={
#    /ip dhcp-client remove [/interface dhcp-client find interface=ether1]
#}
#:if ([/interface dhcp-client find interface=wlan1] != "") do={
#    /ip dhcp-client remove [/interface dhcp-client find interface=wlan1]
#}
#:if ([/ip address find interface=ether1] != "") do={
#    /ip address remove [/ip address find interface=ether1]
#}
/ip address add address=10.31.69.$floor0$idnum/28 network=10.31.68.1 interface=bridge1
}
